1.  unzip -d contract ../../maven-projects/grpc-protolib.zip 
-> write proto
-> in pom.xml make changes as per your taste
    <modelVersion>4.0.0</modelVersion>
    <groupId>samples.java.app</groupId>
    <artifactId>contract-emps</artifactId>
    <version>1.0.0</version>

2.  unzip -d server ../maven-projects/eclink-app.zip
-> in pom.xml 
     add your contract project as dependency
     in my case 
     <dependency>
         <groupId>samples.java.app</groupId>
         <artifactId>contract-emps</artifactId>
         <version>1.0.0</version>
       </dependency>
-> and change your servers project groupId and artifactId for server too.


3. in contarct
mvn install
mvn clean compile

4.
ls ~/.m2/repository/samples/java/app/contract-emps/1.0.0/
rm ~/.m2/repository/samples/java/app/contract-emps/1.0.0/*.lastUpdated
5.
 in contract 
mvn clean install

6.
 protoc --version
  ->  sudo apt update
  ->  sudo apt install protobuf-compiler
  ->  protoc --version
  ->  mvn clean compile
  ->  mvn clean
  ->  mvn compile
  ->  mvn clean compile
  ->  cd contract/
  ->  mvn install -q
  ->  mvn clean install -U
  ->  ls cd ~/.m2/repository/samples/java/app/contract-emps/1.0.0/
  ->  rm ~/.m2/repository/samples/java/app/contract-emps/1.0.0/*.lastUpdated
  ->  mvn clean install -q
  -> cd server
  ->  ./run

7.  
 ---------create api
unzip -d rest ../maven-projects/jersey-restsvc.zip 

-> add dependecy
  <dependency>
         <groupId>samples.java.app</groupId>
         <artifactId>contract-emps</artifactId>
         <version>1.0.0</version>
       </dependency>
